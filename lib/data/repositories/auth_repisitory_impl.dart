import 'package:auth_app/domain/repositories/auth_repository.dart';
import 'package:dartz/dartz.dart';

class AuthRepisitoryImpl implements AuthRepository {
  @override
  Future<Either> signup() {
    throw UnimplementedError();
  }
}
